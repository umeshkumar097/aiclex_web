import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Free AI Social Media Marketing Assistant | Aiclex",
  description: "Generate professional social media content and strategies for the Indian market instantly with Aiclex AI. Free, fast, and high-converting.",
};

export default function AiMarketingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
